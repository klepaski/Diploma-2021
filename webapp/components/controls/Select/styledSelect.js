import styled from "styled-components";
import { space } from 'styled-system'

const SelectWrapper = styled.div`
   ${space}
   
   .programmer-select > div {
      min-height: 46px;
   }
`;

export {SelectWrapper}